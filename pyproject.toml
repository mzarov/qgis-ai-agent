[tool.poetry]
name = "qgis-ai-agent"
version = "0.4.0"
description = "Плагин QGIS 4: ИИ-агент с инструментами чтения проекта и обработки данных"
authors = ["mzarov@duck.com"]

[tool.poetry.dependencies]
python = "^3.12"
keyring = "^25.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
