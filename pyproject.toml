[tool.poetry]
name = "qgis-ai-agent"
version = "0.5.0"
description = "QGIS 4 plugin: an AI agent with tools for reading the project and processing data"
authors = ["mzarov@duck.com"]

[tool.poetry.dependencies]
python = "^3.12"
keyring = "^25.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
